version: 2

sphinx:
  configuration: docs/conf.py

build:
  os: ubuntu-24.04
  apt_packages:
    - mold
    - doxygen
  tools:
    python: "3.12"

  jobs:
    post_checkout:
      # Skip docs build if the commit message contains "skip ci"
      - (git --no-pager log --pretty="tformat:%s -- %b" -1 | grep -viq "skip ci") || exit 183
      # Skip docs build if there are no changes related to docs
      - |
        if [ "$READTHEDOCS_VERSION_TYPE" = "external" ] && git diff --quiet origin/main -- docs/ .readthedocs.yaml noxfile.py pyproject.toml uv.lock CMakePresets.json include/fiction/ bindings/mnt/;
        then
          exit 183;
        fi
      # Unshallow the git clone and fetch tags to get proper version information
      - git fetch --unshallow --tags
    pre_build:
      # Set up uv
      - asdf plugin add uv
      - asdf install uv latest
      - asdf global uv latest
    build:
      html:
        - PYTHONMALLOC=malloc uvx nox --non-interactive -s docs
        - mkdir -p $READTHEDOCS_OUTPUT/html && cp -R docs/_build/html/. $READTHEDOCS_OUTPUT/html
